# Coding Agent CLI - Полная документация

Автоматизированная система генерации и рецензирования кода на базе LangGraph и OpenRouter.

## Установка

```bash
# Клонирование репозитория
git clone <repo-url>
cd CodingAgent

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env и добавьте OPENROUTER_API_KEY и GITHUB_TOKEN
```

## Быстрый старт

```bash
# 1. Запуск сервера
coding-agent server start

# 2. В другом терминале - выполнение задачи
coding-agent run --repo owner/repo --issue 123
```

---

## Глобальные опции

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера (auto-discover если не указан) |
| `--help` | `-h` | Показать справку |
| `--version` | `-V` | Показать версию |

---

## Основные команды

### `run` - Запуск агента с live dashboard

Основная команда для выполнения задач по генерации кода из GitHub issues.

```bash
coding-agent run --repo <repo> --issue <number> [options]
```

| Опция | Короткая | Описание | Обязательный |
|-------|----------|----------|--------------|
| `--repo` | `-r` | URL репозитория (owner/repo или полный URL) | Да |
| `--issue` | `-i` | Номер GitHub issue | Да |
| `--token` | `-t` | GitHub токен (использует GITHUB_TOKEN env) | Нет |
| `--branch` | `-b` | Название ветки для изменений | Нет |
| `--max-iterations` | `-m` | Максимальное количество итераций (по умолчанию: 5) | Нет |
| `--server` | `-s` | URL сервера | Нет |

**Примеры:**

```bash
# Базовое использование
coding-agent run --repo facebook/react --issue 123

# С кастомной веткой
coding-agent run --repo owner/repo --issue 42 --branch feature/auth

# С указанием токена
coding-agent run -r owner/repo -i 42 -t ghp_xxxxxxxxxxxx

# С удаленным сервером
coding-agent run -s https://api.example.com -r owner/repo -i 42

# С увеличенным количеством итераций
coding-agent run -r owner/repo -i 42 -m 10
```

**Формат ветки:** `agent/issue-{issue_number}` (например: `agent/issue-42`)

---

### `tasks` - Список всех задач

Показывает список всех задач с фильтрацией по статусу.

```bash
coding-agent tasks [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |
| `--status` | | Фильтр по статусу (pending/running/completed/failed) |
| `--active-only` | | Показать только активные задачи |

**Примеры:**

```bash
# Все задачи
coding-agent tasks

# Только активные
coding-agent tasks --active-only

# Только завершенные
coding-agent tasks --status completed

# Только ошибки
coding-agent tasks --status failed
```

**Статусы задач:**
- `pending` - Ожидает выполнения
- `running` - Выполняется
- `reviewing` - На рецензировании
- `completed` - Завершена
- `failed` - С ошибкой
- `cancelled` - Отменена

---

### `status` - Статус задачи

Показывает детальную информацию о конкретной задаче.

```bash
coding-agent status <task_id> [options]
```

| Аргумент | Описание |
|----------|----------|
| `task_id` | ID задачи |

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent status abc123-def456-ghi789
coding-agent status abc123 -s https://api.example.com
```

**Вывод включает:**
- Тип задачи (code-agent/reviewer)
- Статус с цветовой индикацией
- Прогресс в процентах
- Текущий шаг
- Номер итерации
- Время создания и продолжительность
- Последние логи

---

### `logs` - Логи задачи

Показывает логи выполнения задачи.

```bash
coding-agent logs <task_id> [options]
```

| Аргумент | Описание |
|----------|----------|
| `task_id` | ID задачи |

| Опция | Короткая | Описание | По умолчанию |
|-------|----------|----------|--------------|
| `--server` | `-s` | URL сервера | - |
| `--limit` | `-n` | Количество записей лога | 50 |

**Примеры:**

```bash
# Последние 50 логов
coding-agent logs abc123-def456

# Последние 100 логов
coding-agent logs abc123 -n 100

# Все логи
coding-agent logs abc123 -n 9999
```

---

### `diff` - Изменения файлов задачи

Показывает diff изменений файлов, созданных задачей.

```bash
coding-agent diff <task_id> [options]
```

| Аргумент | Описание |
|----------|----------|
| `task_id` | ID задачи |

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent diff abc123-def456
```

**Вывод включает:**
- Список измененных файлов
- Полный diff с подсветкой синтаксиса
- Номера строк

---

### `stop` - Остановка задачи

Останавливает или отменяет выполняющуюся задачу.

```bash
coding-agent stop <task_id> [options]
```

| Аргумент | Описание |
|----------|----------|
| `task_id` | ID задачи |

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent stop abc123-def456
```

---

## Команды сервера

### `server start` - Запуск API сервера

Запускает FastAPI сервер для обработки задач.

```bash
coding-agent server start [options]
```

| Опция | Короткая | Описание | По умолчанию |
|-------|----------|----------|--------------|
| `--host` | `-h` | Хост для привязки | 0.0.0.0 |
| `--port` | `-p` | Порт | 8000 |
| `--reload` | | Автоматическая перезагрузка при изменениях | false |

**Примеры:**

```bash
# Стандартный запуск
coding-agent server start

# С кастомным портом
coding-agent server start -p 8080

# С auto-reload (для разработки)
coding-agent server start --reload

# Только localhost
coding-agent server start -h 127.0.0.1
```

**Эндпоинты сервера:**
- `GET /health` - Проверка работоспособности
- `GET /docs` - Swagger документация
- `POST /tasks` - Создание задачи
- `GET /tasks` - Список задач
- `GET /tasks/{task_id}` - Детали задачи
- `GET /tasks/{task_id}/logs` - Логи задачи
- `GET /tasks/{task_id}/diff` - Diff изменений
- `DELETE /tasks/{task_id}` - Отмена задачи
- `WS /ws/tasks/{task_id}` - WebSocket для real-time обновлений

---

### `server status` - Проверка статуса сервера

Проверяет работоспособность сервера.

```bash
coding-agent server status [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера для проверки |

**Примеры:**

```bash
# Auto-discover сервер
coding-agent server status

# Проверить конкретный сервер
coding-agent server status -s https://api.example.com
```

**Вывод включает:**
- Статус (healthy/degraded)
- Версию
- Конфигурацию OpenRouter
- Конфигурацию GitHub
- Подключение Redis
- Количество активных задач

---

## Команды конфигурации

### `config list` - Список конфигурации

Показывает текущую конфигурацию CLI.

```bash
coding-agent config list [options]
```

| Опция | Описание |
|-------|----------|
| `--show-tokens` | Показать сохраненные токены (замаскированные) |

**Примеры:**

```bash
# Базовая конфигурация
coding-agent config list

# С отображением токенов
coding-agent config list --show-tokens
```

**Файл конфигурации:** `~/.coding-agent/config.json`

---

### `config add-server` - Добавить сервер

Добавляет сервер в конфигурацию.

```bash
coding-agent config add-server <name> <url> [options]
```

| Аргумент | Описание |
|----------|----------|
| `name` | Имя сервера (для ссылки) |
| `url` | URL сервера |

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--description` | `-d` | Описание сервера |
| `--set-default` | | Установить как сервер по умолчанию |

**Примеры:**

```bash
# Добавить сервер
coding-agent config add-server mycloud https://api.example.com

# С описанием и сделать по умолчанию
coding-agent config add-server production https://api.prod.com -d "Production server" --set-default
```

---

### `config remove-server` - Удалить сервер

Удаляет сервер из конфигурации.

```bash
coding-agent config remove-server <name>
```

| Аргумент | Описание |
|----------|----------|
| `name` | Имя сервера |

**Примеры:**

```bash
coding-agent config remove-server mycloud
```

---

### `config set-default` - Установить сервер по умолчанию

Устанавливает сервер по умолчанию.

```bash
coding-agent config set-default <name>
```

| Аргумент | Описание |
|----------|----------|
| `name` | Имя сервера |

**Примеры:**

```bash
coding-agent config set-default production
```

---

### `config add-token` - Сохранить токен

Сохраняет GitHub токен для сервера.

```bash
coding-agent config add-token <server_url> <token>
```

| Аргумент | Описание |
|----------|----------|
| `server_url` | URL сервера |
| `token` | GitHub токен |

**Примеры:**

```bash
coding-agent config add-token https://api.example.com ghp_xxxxxxxxxxxx
```

---

### `config remove-token` - Удалить токен

Удаляет сохраненный токен.

```bash
coding-agent config remove-token <server_url>
```

| Аргумент | Описание |
|----------|----------|
| `server_url` | URL сервера |

**Примеры:**

```bash
coding-agent config remove-token https://api.example.com
```

---

## Команды мониторинга репозиториев

### `monitor add` - Добавить репозиторий на мониторинг

Добавляет репозиторий для автоматического отслеживания новых issues.

```bash
coding-agent monitor add <owner> <repo> [options]
```

| Аргумент | Описание |
|----------|----------|
| `owner` | Владелец репозитория (организация или пользователь) |
| `repo` | Название репозитория |

| Опция | Короткая | Описание | По умолчанию |
|-------|----------|----------|--------------|
| `--token` | `-t` | GitHub токен для доступа | Обязательный |
| `--interval` | `-i` | Интервал опроса в секундах | 60 |
| `--server` | `-s` | URL сервера | - |

**Примеры:**

```bash
# Базовое добавление
coding-agent monitor add facebook react --token ghp_xxx

# С кастомным интервалом
coding-agent monitor add owner repo -t ghp_xxx -i 120

# Для удаленного сервера
coding-agent monitor add owner repo -t ghp_xxx -s https://api.example.com
```

---

### `monitor remove` - Удалить репозиторий из мониторинга

Удаляет репозиторий из отслеживания.

```bash
coding-agent monitor remove <owner> <repo> [options]
```

| Аргумент | Описание |
|----------|----------|
| `owner` | Владелец репозитория |
| `repo` | Название репозитория |

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent monitor remove facebook react
```

---

### `monitor list` - Список отслеживаемых репозиториев

Показывает все репозитории на мониторинге.

```bash
coding-agent monitor list [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent monitor list
```

**Вывод включает:**
- Репозиторий (owner/repo)
- Статус (включен/выключен)
- Интервал опроса
- Время последней проверки
- Количество обработанных issues

---

### `monitor status` - Статус мониторинга

Показывает текущий статус мониторинга.

```bash
coding-agent monitor status [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent monitor status
```

---

### `monitor start` - Запустить мониторинг

Запускает фоновый процесс мониторинга для всех добавленных репозиториев.

```bash
coding-agent monitor start [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent monitor start
```

---

### `monitor stop` - Остановить мониторинг

Останавливает процесс мониторинга.

```bash
coding-agent monitor stop [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--server` | `-s` | URL сервера |

**Примеры:**

```bash
coding-agent monitor stop
```

---

## Legacy команды (обратная совместимость)

### `code-agent` - Прямой запуск Code Agent

Устаревшая команда для прямого запуска агента. Рекомендуется использовать `run`.

```bash
coding-agent code-agent <issue_number> [options]
```

| Опция | Короткая | Описание | По умолчанию |
|-------|----------|----------|--------------|
| `--branch` | `-b` | Название ветки | auto |
| `--max-iterations` | `-m` | Макс. итераций | 5 |
| `--output` | `-o` | Сохранить результат в JSON файл | - |

**Примеры:**

```bash
coding-agent code-agent 123
coding-agent code-agent 123 -b feature/auth -m 7 -o result.json
```

---

### `reviewer` - Прямой запуск Reviewer Agent

Устаревшая команда для рецензирования PR.

```bash
coding-agent reviewer <pr_number> [options]
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--issue-number` | `-i` | Связанный номер issue |
| `--output` | `-o` | Сохранить результат в JSON файл |

**Примеры:**

```bash
coding-agent reviewer 456
coding-agent reviewer 456 -i 123 -o review.json
```

---

### `show-issue` - Показать детали Issue

```bash
coding-agent show-issue <issue_number>
```

**Примеры:**

```bash
coding-agent show-issue 123
```

---

### `show-pr` - Показать детали PR

```bash
coding-agent show-pr <pr_number>
```

**Примеры:**

```bash
coding-agent show-pr 456
```

---

## Переменные окружения

| Переменная | Описание |
|------------|----------|
| `OPENROUTER_API_KEY` | API ключ OpenRouter |
| `GITHUB_TOKEN` | GitHub токен |
| `REDIS_URL` | URL Redis подключения |
| `GITHUB_REPO` | Репозиторий по умолчанию (owner/repo) |
| `CODING_AGENT_SERVER` | URL сервера по умолчанию |
| `API_HOST` | Хост API сервера |
| `API_PORT` | Порт API сервера |
| `DEBUG` | Режим отладки |

---

## Auto-discover сервера

CLI автоматически определяет сервер в следующем порядке:

1. **Environment variable**: `CODING_AGENT_SERVER`
2. **Config file**: `~/.coding-agent/config.json` → `default_server`
3. **Localhost**: `http://localhost:8000` (проверка health)
4. **Fallback**: `http://localhost:8000`

---

## Remote сервера

### Localtunnel (рекомендуется)

```bash
# Установка
npm install -g localtunnel

# Запуск tunnel
lt --port 8000

# Использование
export CODING_AGENT_SERVER=https://random-name.loca.lt
coding-agent run -r owner/repo -i 123
```

### Ngrok

```bash
# Запуск tunnel
ngrok http 8000

# Использование
export CODING_AGENT_SERVER=https://abc123.ngrok.io
coding-agent run -r owner/repo -i 123
```

---

## Live Dashboard

При выполнении команды `run` автоматически открывается live dashboard с:

- Прогресс-бар выполнения
- Текущий шаг операции
- Real-time логи
- Статистику итераций
- URL созданного PR

**Горячие клавиши:**
- `R` - Retry (повторить)
- `S` - Stop (остановить)
- `D` - Diff (показать изменения)
- `L` - Logs (логи)
- `Q` - Quit (выйти)

---

## Примеры использования

### Полный workflow

```bash
# 1. Настройка конфигурации
coding-agent config add-server production https://api.example.com --set-default
coding-agent config add-token https://api.example.com ghp_xxx

# 2. Запуск сервера
coding-agent server start --reload

# 3. В другом терминале - выполнение задачи
coding-agent run --repo owner/repo --issue 123

# 4. Проверка статуса
coding-agent status <task_id>

# 5. Просмотр изменений
coding-agent diff <task_id>
```

### Мониторинг репозитория

```bash
# 1. Добавить репозиторий на мониторинг
coding-agent monitor add owner repo --token ghp_xxx --interval 60

# 2. Запустить мониторинг
coding-agent monitor start

# 3. Проверить статус
coding-agent monitor status

# 4. Список отслеживаемых
coding-agent monitor list
```

---

## Работа с удаленным сервером

### Что такое remote сервер

Remote сервер позволяет запускать Coding Agent на одной машине (сервере), а управлять им с другой машины (клиента). Полезно для:
- Использования более мощного сервера для генерации кода
- Доступа к агенту из любой точки мира
- Совместной работы команды

### Сценарии развертывания

```
1. LOCAL:      docker-compose up → localhost:8000
2. REMOTE:     ngrok/server → https://abc123.ngrok.io
3. CLOUD:      Yandex Cloud / cloud.ru → https://api.example.com
```

### Способы подключения к remote серверу

#### Способ 1: Переменная окружения

```bash
export CODING_AGENT_SERVER=https://api.example.com
coding-agent run -r owner/repo -i 123
```

#### Способ 2: Опция --server

```bash
coding-agent run -s https://api.example.com -r owner/repo -i 123
```

#### Способ 3: Config file

```bash
# Добавить сервер в конфигурацию
coding-agent config add-server mycloud https://api.example.com --set-default

# Использовать (автоматически определится)
coding-agent run -r owner/repo -i 123
```

### Развертывание remote сервера

#### Вариант A: Localtunnel (рекомендуется, бесплатно)

```bash
# 1. Установка localtunnel (один раз)
npm install -g localtunnel

# 2. Запуск Coding Agent сервера
coding-agent server start

# 3. В другом терминале - запуск tunnel
lt --port 8000

# Получите URL: https://random-name.loca.lt

# 4. На любой другой машине (даже без Docker)
export CODING_AGENT_SERVER=https://random-name.loca.lt
coding-agent run -r owner/repo -i 123
```

#### Вариант B: Ngrok

```bash
# 1. Запуск Coding Agent сервера
coding-agent server start

# 2. В другом терминале - запуск ngrok
ngrok http 8000

# Получите URL: https://abc123.ngrok.io

# 3. На любой другой машине
export CODING_AGENT_SERVER=https://abc123.ngrok.io
coding-agent run -r owner/repo -i 123
```

#### Вариант C: VPS / Облачный сервер

```bash
# 1. Подключиться к VPS
ssh user@vps-server.com

# 2. Установить Docker и клонировать репо
git clone <repo-url>
cd CodingAgent
docker-compose up -d

# 3. Настроить reverse proxy (nginx + certbot для HTTPS)
# Пример конфигурации nginx:
server {
    listen 443 ssl;
    server_name api.example.com;

    ssl_certificate /etc/letsencrypt/live/api.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.example.com/privkey.pem;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# 4. Использование с локальной машины
export CODING_AGENT_SERVER=https://api.example.com
coding-agent run -r owner/repo -i 123
```

### Remote dashboard

При подключении к remote серверу dashboard работает так же, как с локальным:

```
┌─ Remote: https://abc123.loca.lt ─ Task #7b4f ───────┐
│ 🔴 Server: https://abc123.loca.lt [✓ Healthy]      │
│ Latency: 45ms | Region: EU                           │
├──────────────────────────────────────────────────────┤
│ Status: [Iteration 3/5] Code Review... 92%           │
│ PR: https://github.com/.../pull/15 [OPEN]            │
└── [R]etry [S]top [D]iff [L]ogs [Q]uit ──────────────┘
```

### Health check remote сервера

```bash
coding-agent server status -s https://api.example.com
```

Вывод:
```
┌─ Status: HEALTHY ───────────────────────────────────┐
│                                                      │
│ Version: 0.1.0                                       │
│ OpenRouter: ✓                                        │
│ GitHub: ✓                                            │
│ Redis: ✓                                             │
│ Active Tasks: 2                                      │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### Хранение токенов для remote серверов

```bash
# Сохранить токен для конкретного сервера
coding-agent config add-token https://api.example.com ghp_xxxxxxxxxxxx

# Токен будет автоматически использоваться для этого сервера
coding-agent run -s https://api.example.com -r owner/repo -i 123
```

---

## Мониторинг репозиториев

### Что такое мониторинг

Мониторинг репозиториев позволяет автоматически:
- Отслеживать появление новых issues
- Запускать Code Agent для обработки
- Создавать Pull Requests без ручного вмешательства

**Workflow:**
```
GitHub Issue создан → Monitor обнаруживает → Code Agent запускается → PR создается
```

### Добавление репозитория на мониторинг

#### Шаг 1: Запустите сервер

```bash
coding-agent server start
```

#### Шаг 2: Добавьте репозиторий

```bash
# Базовая команда
coding-agent monitor add <owner> <repo> --token <github_token>

# Примеры:
coding-agent monitor add facebook react --token ghp_1234567890abcdef
coding-agent monitor add my-org my-project --token ghp_xxxxxxxxxxxx
```

#### Параметры команды:

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `owner` | Да | Владелец репозитория (организация или пользователь) |
| `repo` | Да | Название репозитория |
| `--token`, `-t` | Да | GitHub токен с доступом к репозиторию |
| `--interval`, `-i` | Нет | Интервал опроса в секундах (по умолчанию: 60) |
| `--server`, `-s` | Нет | URL сервера (auto-discover если не указан) |

#### Полный пример:

```bash
# Добавить репозиторий с проверкой каждые 2 минуты
coding-agent monitor add \
    owner \
    repo \
    --token ghp_1234567890abcdef \
    --interval 120 \
    --server http://localhost:8000
```

### Запуск мониторинга

```bash
# Запустить мониторинг всех добавленных репозиториев
coding-agent monitor start

# С проверкой сервера
coding-agent monitor start -s https://api.example.com
```

После запуска:
- Monitor будет опрашивать GitHub API каждые N секунд
- Новые issues автоматически обрабатываются
- Создаются Pull Requests

### Проверка статуса

```bash
# Статус мониторинга
coding-agent monitor status

# Список всех отслеживаемых репозиториев
coding-agent monitor list
```

Пример вывода `monitor list`:

```
┌─ Monitored Repositories ──────────────────────────────┐
│                                                       │
│ Repository          │ Enabled │ Interval │ Last Check  │
├─────────────────────┼─────────┼──────────┼─────────────┤
│ facebook/react      │ ✓       │ 60s      │ 2 min ago   │
│ my-org/backend      │ ✓       │ 120s     │ 5 min ago   │
│ other/project       │ ✗       │ 60s      │ Never       │
│                                                       │
└───────────────────────────────────────────────────────┘

Total: 3 repositories
```

### Удаление репозитория

```bash
coding-agent monitor remove <owner> <repo>

# Пример:
coding-agent monitor remove facebook react
```

### Остановка мониторинга

```bash
coding-agent monitor stop
```

### Полный workflow мониторинга

```bash
# 1. Запуск сервера (терминал 1)
coding-agent server start

# 2. Добавление репозиториев (терминал 2)
coding-agent monitor add owner backend --token ghp_xxx
coding-agent monitor add owner frontend --token ghp_xxx

# 3. Запуск мониторинга
coding-agent monitor start

# 4. Проверка статуса
coding-agent monitor status

# 5. Список отслеживаемых репозиториев
coding-agent monitor list

# 6. Когда новые issues появляются, они обрабатываются автоматически
# Проверьте созданные PRs на GitHub
```

### Мониторинг с remote сервера

```bash
# 1. На сервере: запустить monitoring
coding-agent monitor start

# 2. С клиента: проверить статус
coding-agent monitor status -s https://api.example.com

# 3. Добавить репозиторий удаленно
coding-agent monitor add owner repo -t ghp_xxx -s https://api.example.com
```

### GitHub Permissions

Токен должен иметь права:
- `repo` (full access)
- `issues: read` - для чтения issues
- `pull_requests: write` - для создания PR

Создание токена: https://github.com/settings/tokens

### Автоматическая обработка issues

Когда мониторинг запущен:

1. **Обнаружение**: Новые issues обнаруживаются через GitHub API
2. **Парсинг**: Issue анализируется на тип (feature/bugfix/...)
3. **Генерация**: Code Agent создает код
4. **PR**: Pull Request создается автоматически
5. **Review**: Reviewer Agent проверяет код

Репозиторий отслеживания:
- Модуль: `app/core/monitoring.py`
- API: `app/api/monitoring.py`
- CLI: `monitor` команды

---

## Устранение проблем

### Сервер не запускается

```bash
# Проверить порт
lsof -i :8000

# Использовать другой порт
coding-agent server start -p 8080
```

### Задача зависла

```bash
# Остановить задачу
coding-agent stop <task_id>

# Проверить логи
coding-agent logs <task_id> -n 100
```

### Connection refused

```bash
# Проверить статус сервера
coding-agent server status

# Указать сервер явно
coding-agent run -s http://localhost:8000 -r owner/repo -i 123
```

---

## Дополнительная информация

- **Версия**: `coding-agent --version`
- **Справка**: `coding-agent --help`
- **Справка команды**: `coding-agent <command> --help`
- **API документация**: http://localhost:8000/docs (при запущенном сервере)

---

## Статус системы

### Версия
**v0.1.0** (2026-01-30)

### Протестированные компоненты

| Компонент | Статус | Примечание |
|-----------|--------|------------|
| CLI команды | ✅ | Все команды работают |
| Code Agent | ✅ | Генерация кода функциональна |
| Reviewer Agent | ✅ | Рецензирование PR работает |
| Мониторинг | ✅ | Полный цикл протестирован |
| API сервер | ✅ | FastAPI endpoints работают |
| WebSocket | ✅ | Real-time обновления работают |
| Промпты | ✅ | С примерами для структурированных ответов |
| LLM интеграция | ✅ | OpenRouter с бесплатными моделями |

### Бесплатные модели OpenRouter

| Модель | Использование | Статус |
|--------|---------------|--------|
| `qwen/qwen-2.5-coder-32b-instruct` | Coding, Planning | ✅ |
| `google/gemini-flash-1.5` | Review | ✅ |
| `deepseek/deepseek-coder` | Coding (backup) | ✅ |
| `meta-llama/llama-3-70b-instruct` | Качественные задачи | ✅ |

### Known Limitations

1. **GitHub API Rate Limiting** - без токена или с базовым токеном есть лимиты запросов
2. **Redis Requirement** - для персистентности необходим запущенный Redis сервер
3. **Background Processing** - требуется запущенный API сервер для работы мониторинга

---

## Тестирование компонентов

### Тест мониторинга

Для проверки мониторинга используйте:

```bash
# 1. Запуск сервера
coding-agent server start

# 2. Добавление тестового репозитория
coding-agent monitor add owner repo --token ghp_xxx

# 3. Проверка списка
coding-agent monitor list

# 4. Запуск мониторинга
coding-agent monitor start

# 5. Проверка статуса
coding-agent monitor status
```

### Тест LLM

Проверка работы с OpenRouter:

```bash
source venv/bin/activate
python -c "
from app.core.llm.openrouter import list_free_models
models = list_free_models()
for m in models:
    print(f'{m[\"key\"]}: {m[\"model_id\"]}')
"
```

### Тест промптов

Проверка промптов:

```bash
source venv/bin/activate
python -c "
from app.core.llm.prompts import ISSUE_PARSER_PROMPT
messages = ISSUE_PARSER_PROMPT.format_messages(
    issue_number=1,
    issue_title='Test',
    issue_body='Test body'
)
print('System prompt length:', len(messages[0].content))
print('Has examples:', 'ПРИМЕР' in messages[0].content)
"
```

---

## Архитектура

### Модули

```
app/
├── api/
│   ├── main.py              # FastAPI приложение
│   ├── tasks.py             # Эндпоинты задач
│   ├── monitoring.py        # Эндпоинты мониторинга ✨ NEW
│   ├── websocket.py         # WebSocket обработчики
│   └── streaming.py         # SSE streaming
├── cli/
│   ├── main.py              # CLI команды (включая monitor) ✨ NEW
│   ├── dashboard.py         # Live dashboard
│   ├── config.py            # Управление конфигурацией
│   └── utils.py             # CLI утилиты
├── core/
│   ├── monitoring.py        # Модуль мониторинга ✨ NEW
│   ├── agents/
│   │   ├── code_agent.py    # Code Agent workflow
│   │   └── reviewer_agent.py # Reviewer workflow
│   ├── llm/
│   │   ├── openrouter.py    # OpenRouter клиент (бесплатные модели) ✨ NEW
│   │   └── prompts.py       # Промпты с примерами ✨ NEW
│   ├── tools/
│   │   └── github_tools.py  # LangChain инструменты
│   └── task_manager.py      # Управление задачами
```

### API Эндпоинты

**Tasks:**
- `POST /tasks` - Создать задачу
- `GET /tasks` - Список задач
- `GET /tasks/{task_id}` - Детали задачи
- `DELETE /tasks/{task_id}` - Отменить задачу
- `GET /tasks/{task_id}/logs` - Логи
- `GET /tasks/{task_id}/diff` - Diff изменений

**Monitoring ✨ NEW:**
- `POST /monitoring/repos` - Добавить репозиторий
- `GET /monitoring/repos` - Список репозиториев
- `DELETE /monitoring/repos/{owner}/{repo}` - Удалить репозиторий
- `GET /monitoring/status` - Статус мониторинга
- `POST /monitoring/start` - Запустить мониторинг
- `POST /monitoring/stop` - Остановить мониторинг

---

## Changelog

### v0.1.0 (2026-01-30)

**Added:**
- ✅ Мониторинг репозиториев (CLI + API)
- ✅ Бесплатные модели OpenRouter
- ✅ Промпты с примерами для структурированных ответов
- ✅ Remote сервер поддержка (localtunnel, ngrok, VPS)
- ✅ Live dashboard с hot keys
- ✅ WebSocket для real-time обновлений

**Fixed:**
- ✅ Redis scan метод для мониторинга
- ✅ Сериализация MonitoredRepo

**Improved:**
- ✅ Auto-discover сервера
- ✅ Управление конфигурацией CLI
- ✅ Сохранение токенов для разных серверов

---

## Поддержка

Для вопросов и проблем:
- GitHub Issues: https://github.com/your-repo/issues
- Документация: `CLI.md`, `MONITORING_TEST.md`, `README.md`
- API Docs: http://localhost:8000/docs
